import {IconButton} from "@chakra-ui/react";
import {GithubIcon} from "./Icons";

export const GithubIconButton = () => <IconButton aria-label="github button" isRound icon={<GithubIcon fontSize="1.6rem" />} />;
